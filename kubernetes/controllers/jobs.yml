apiVersion: batch/v1
kind: Job
metadata:
  name: pi
spec:
  template:
    spec:
      containers:
      - name: busybox
        image: busybox
        command: ["/bin/bash", "-c", "sleep 5"]
      restartPolicy: Never
